{
    "version": 2,
    "builds": [
      {
        "src": "api/index.ts",
        "use": "@vercel/node"
      }
    ],
    "routes": [
      {
        "src": "/soso/(.*)",
        "dest": "api/index.ts"
      }
    ]
  }
// { "version": 2, "rewrites": [{ "source": "/(.*)", "destination": "/api" }] }